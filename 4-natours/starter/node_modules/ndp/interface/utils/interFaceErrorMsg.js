const interFaceErrorMsg = (interfaceName, interfacePath, queryPath) =>
  `接口 ${interfaceName}：${interfacePath}数据错误：\n${queryPath
    .split(',')
    .join('->')}`;
module.exports = interFaceErrorMsg;
